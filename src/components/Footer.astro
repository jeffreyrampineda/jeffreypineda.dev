---
import { Image } from 'astro:assets';

const today = new Date();

const socialLinks = [
  {
    name: 'instagram',
    href: 'https://www.instagram.com/jeffreyrampineda/',
  },
  {
    name: 'stackoverflow',
    href: 'https://stackoverflow.com/users/8550657/jeffrey-ram/',
  },
  {
    name: 'github',
    href: 'https://github.com/jeffreyrampineda/',
  },
  {
    name: 'linkedin',
    href: 'https://www.linkedin.com/in/jeffreyrampineda/',
  },
];
---

<footer
  id="footer"
  class="w-full container mx-auto px-3 py-8 md:px-6 md:py-16 xl:px-32 text-center md:text-left"
>
  <div class="flex flex-col md:flex-row gap-y-16 gap-x-2">
    <div class="grow md:w-3/6 flex flex-col gap-y-4">
      <a class="flex justify-center md:justify-start" href="/">
        <Image
          src="/assets/logo.png"
          width={42}
          height={42}
          alt="Logo of Jeffrey Ram Pineda"
        />
      </a>
      <p class="max-w-md font-light tracking-wide">
        Although I may not be able to respond immediately, my inbox is always
        open. Whether you have a question or just want to say hi, I&apos;ll try
        my best to get back to you!
      </p>
      <div class="flex flex-row gap-4 justify-center md:justify-start">
        {
          socialLinks.map((item) => (
            <a href={item.href}>
              <Image
                class="invert-[.5]"
                src={`/assets/icons/icon_${item.name}.svg`}
                alt={item.name}
                width={24}
                height={24}
              />
            </a>
          ))
        }
      </div>
    </div>
    <div class="grow md:w-1/6 flex flex-col gap-y-4 font-light tracking-wide">
      <h3 class="font-bold">Socials</h3>
      {
        socialLinks.map((item) => (
          <a class="capitalize" href={item.href}>
            {item.name}
          </a>
        ))
      }
    </div>
    <div class="grow md:w-1/6 flex flex-col gap-y-4 font-light tracking-wide">
      <h3 class="font-bold">Contact Me</h3>
      <p>Toronto, Ontario</p>
      <p>
        <a class="text-sky-300" href="mailto:jeffreyrampineda@gmail.com">
          jeffreyrampineda@gmail.com
        </a>
      </p>
    </div>
  </div>
  <p class="pt-4 font-light tracking-wide">
    &copy; {today.getFullYear()} Jeffrey Ram Pineda. All rights reserved.
  </p>
</footer>
